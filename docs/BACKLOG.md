# Backlog

Open follow-up items for `xian-wallet-mobile`:

- decide where shared wallet-core logic should live before unifying mobile and
  browser wallet signing behavior
- expand network preset handling after future public network manifests exist
- add release-signing runbooks for Android and iOS once distribution channels
  are finalized
- keep WalletConnect approval prompts aligned with the browser wallet provider
  as provider request shapes evolve
- add more device-level QA coverage for secure storage, biometrics, camera,
  share sheet, and offline recovery flows

Keep this file limited to repo-local follow-ups. Cross-repo wallet-core design
should live in `xian-meta` or the eventual owning shared package.
